---
status: active
---

# tropo

The typed-knowledge layer. This file's type is `project` because its nearest
registered ancestor folder is `projects/`. Note that the file two folders down —
`decisions/0001-folder-as-type.md` — is **not** a project: its nearest
registered ancestor is `decisions/`, so it resolves to type `decision`. The tree
carries the types; nesting just works.
