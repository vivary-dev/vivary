---
relationship: self
---

# Jeff

Builder of tropo. Everything else about this note — its type (`person`, from the
`people/` folder), its `id` (`jeff`, from the filename), its `title` (this H1),
and its `created`/`updated` dates (from git) — is derived. The only thing worth
writing down is the one fact that isn't: the relationship.
