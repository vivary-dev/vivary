---
attendees: [Jeff, Claude]
project: tropo
---

# tropo kickoff

Decided the shape: filesystem is the schema, folder-as-type, derive everything
derivable, tighten-never-loosen composition. The only irreducible facts here are
who was in the room and which project it belongs to — so those are the only two
fields. The `date` is derivable from the filename and git, so it is omitted.
