interface:
  display_name: "Vivary"
  short_description: "Repo-specific patterns and workflows for vivary"
  default_prompt: "Use the vivary repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true