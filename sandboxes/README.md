# sandboxes

Throwaway workspaces to test `create vivary` and the modules against — a
second-brain vault, a coding repo, a writing project. Each is disposable.

Contents are gitignored (see root `.gitignore`); keep nothing precious here.
